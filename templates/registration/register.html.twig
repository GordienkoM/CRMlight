{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block body %}
{# Insertion de navigation d'administration #}
{% embed "admin/index.html.twig" %}{% endembed %}

<div class="uk-flex uk-flex-column uk-flex-center"></div>
    <h1 class="uk-text-center">Ajouter un collaborateur</h1>
    <div class="uk-card uk-card-default uk-card-large uk-card-body uk-container uk-container-small uk-padding">       
        {{ form(registrationForm) }}     
    </div>
</div>
<div class="uk-flex">
    <div class="uk-margin-right">
        <a class="uk-button uk-button-default" href="{{ path('contact_new') }}">Créer un contact</a>
    </div>
    <div>
        <a class="uk-button uk-button-default" href="{{ path('admin_users') }}">Liste des utilisateur</a>
    </div>
</div>
{% endblock %}
